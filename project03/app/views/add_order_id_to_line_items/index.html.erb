<h1>Listing add_order_id_to_line_items</h1>

<table>
  <tr>
    <th>Order</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @add_order_id_to_line_items.each do |add_order_id_to_line_item| %>
  <tr>
    <td><%= add_order_id_to_line_item.order_id %></td>
    <td><%= link_to 'Show', add_order_id_to_line_item %></td>
    <td><%= link_to 'Edit', edit_add_order_id_to_line_item_path(add_order_id_to_line_item) %></td>
    <td><%= link_to 'Destroy', add_order_id_to_line_item, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Add order id to line item', new_add_order_id_to_line_item_path %>
